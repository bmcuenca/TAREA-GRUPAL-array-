paquete  tareaarray ;

import  java.util.Scanner ;

public  class  TareaArray {

    public  static  void  main ( String [] args ) {
        Núm. Escáner =  nuevo  escáner ( System . In);
        int totalsuma = 0 ;
        
        int [] numeros =  nuevo  int [ 2 ];
        
        para ( int i = 0 ; i < numeros . longitud; i ++ ) {
            Sistema . fuera . println ( " Ingrese Número: " );
            numeros [i] = num . nextInt ();
        }
        
        para ( int i = 0 ; i < numeros . longitud; i ++ ) {
            totalsuma = totalsuma + numeros [i];
        }
        Sistema . fuera . println ( " Total de la suma es "  + totalsuma);
    }
    
}
